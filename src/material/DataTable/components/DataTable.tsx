import React, { FC } from 'react';
import DataTableHead from './DataTableHead';

const DataTable: FC = () => {
  return (
    <table>
      <DataTableHead />
    </table>
  );
};

export default DataTable;
